## Instalación rápida

1. Instala Python 3.10+
2. Instala dependencias: `pip install streamlit fastapi pymongo simpy`
3. Ejecuta el backend: `uvicorn backend.main:app --reload`
4. Ejecuta el frontend: `streamlit run frontend/streamlit_app.py`